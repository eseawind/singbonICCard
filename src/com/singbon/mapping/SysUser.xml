<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.singbon.dao.SysUserDAO">
	<resultMap id="sysUser" type="com.singbon.entity.SysUser">
	</resultMap>
	<parameterMap type="com.singbon.entity.SysUser" id="sysUser2" />
	<cache eviction="FIFO" size="2000" readOnly="true"/>
	<select id="selectByOperId" resultMap="sysUser">
		select * from sysuser where operId=#{operId}
	</select>
	<select id="login" resultMap="sysUser" useCache="false">
		select u.* from sysuser u,company c where u.companyId=c.id and u.loginName =#{loginName} and u.loginPwd=#{loginPwd} and c.companyName =#{companyName}
	</select>
	<select id="selectByLoginInfo" resultMap="sysUser">
		select * from sysuser where loginName =#{loginName} and loginPwd =#{loginPwd}
	</select>
	<select id="selectListByCompanyId" resultMap="sysUser">
		select * from sysuser where companyId =#{companyId}
	</select>
	<select id="selectCashierList" resultMap="sysUser">
		select * from sysuser where userType=2 and companyId =#{companyId}
	</select>
	<select id="selectMaxCardNO" resultType="java.lang.Integer" useCache="false">
		select ifnull(max(cardNO),0)+1 cardNO from sysUser where companyId=#{companyId}
	</select>
	<select id="selectCountByCardSN" resultType="java.lang.Integer">
		select count(operId) from sysUser where companyId=#{companyId} and cardSN=#{cardSN}
	</select>
	<update id="makeSpecailCard" parameterMap="sysUser2">
		update sysUser set cardNO=#{cardNO},cardSN=#{cardSN},status=1
		where operId=#{operId}
	</update>
	<update id="changeStatus">
		update sysUser set status=#{status}
		where operId=#{operId}
	</update>
	<update id="changeCashierInvalidDate">
		update sysUser set invalidDate=#{invalidDate}
		where operId=#{operId}
	</update>
</mapper>
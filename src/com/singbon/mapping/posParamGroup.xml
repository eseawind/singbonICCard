<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.singbon.dao.systemManager.systemSetting.PosParamGroupDAO">
	<resultMap id="posParamGroup" type="com.singbon.entity.PosParamGroup">
	</resultMap>
	<parameterMap type="com.singbon.entity.PosParamGroup" id="posParamGroup2" />
	<cache eviction="FIFO" size="1" readOnly="true"/>
	<insert id="insert">
		insert into posParamGroup (companyId,groupName,subsidyReset,subsidyFirst,enableCardMinFare,enableTimeLimitFare,enableDayLimitFare,enableDiscount,enableMeal)
			select #{companyId},#{groupName},subsidyReset,subsidyFirst,enableCardMinFare,enableTimeLimitFare,enableDayLimitFare,enableDiscount,enableMeal from posParamGroupBase
	</insert>
	<update id="update" parameterMap="posParamGroup2">
		update posParamGroup set 
		<if test="groupName!=null">
			groupName=#{groupName}
		</if>
		<if test="groupName==null">
			subsidyReset=#{subsidyReset},subsidyFirst=#{subsidyFirst},enableCardMinFare=#{enableCardMinFare},enableTimeLimitFare=#{enableTimeLimitFare},enableDayLimitFare=#{enableDayLimitFare},enableDiscount=#{enableDiscount},enableMeal=#{enableMeal}			
		</if>
		where id=#{id}
	</update>
	<update id="delete">
		delete from posParamGroup where id=#{id}
	</update>
	<select id="selectById" resultMap="posParamGroup">
		select * from posParamGroup where id=#{id}
	</select>
	<select id="selectListByCompanyId" resultMap="posParamGroup">
		select * from posParamGroup where companyId=#{companyId}
	</select>
	<select id="selectCountWithPos" resultType="java.lang.Integer">
		select count(*) count from device where groupId=#{groupId}
	</select>
</mapper>
